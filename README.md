# MessageMediaApiProxy
A simple API Proxy as the middleware to bypass the brower CORS preflight check when calling MessageMedia API

For code preview, navigate to ./controllers/MessageMediaProxyController.cs
